# 🏗️ Architektura aplikacji Finansowy Tracker

## 📐 Przegląd architektury

Aplikacja jest zbudowana w oparciu o **modularną architekturę** z jasnym podziałem odpowiedzialności:

```
┌─────────────────────────────────────────────┐
│           React Navigation                   │
│  (Routing między ekranami)                   │
└──────────────────┬──────────────────────────┘
                   │
┌──────────────────▼──────────────────────────┐
│           Screens (7 ekranów)               │
│  (Warstwa UI, wyświetlanie danych)         │
└──────────────────┬──────────────────────────┘
                   │
┌──────────────────▼──────────────────────────┐
│        Components (11 komponentów)         │
│  (Elementy UI wielokrotnego użytku)       │
└──────────────────┬──────────────────────────┘
                   │
┌──────────────────▼──────────────────────────┐
│         AppContext (zarządzanie stanem)     │
│  (Globalny stan aplikacji)                 │
└──────────────────┬──────────────────────────┘
                   │
┌──────────────────▼──────────────────────────┐
│      Services & Utils (logika aplikacji)    │
│  - StorageService (AsyncStorage)           │
│  - dateUtils (obsługa dat)                │
│  - moneyUtils (operacje finansowe)        │
│  - validation (walidacja danych)          │
└──────────────────┬──────────────────────────┘
                   │
┌──────────────────▼──────────────────────────┐
│     AsyncStorage (przechowywanie lokalne)   │
│  (Dane na urządzeniu)                      │
└─────────────────────────────────────────────┘
```

## 📂 Структура папок

### `src/screens/` - Экраны приложения

**7 основных экранов:**

1. **SplashScreen.js** (Экран запуска)
   - Отображение логотипа и названия
   - Автоматический переход на Dashboard
   - Loading indicator

2. **DashboardScreen.js** (Главная)
   - Баланс счета
   - Доходы/расходы месяца
   - Топ расходы по категориям
   - Последние операции

3. **AddTransactionScreen.js** (Добавление операции)
   - Форма для добавления операции
   - Валидация данных
   - Выбор категории, даты, типа

4. **TransactionsScreen.js** (Список операций)
   - Хронологический список
   - Фильтрация по типу
   - Удаление/редактирование

5. **CategoriesScreen.js** (Категории)
   - Управление категориями
   - Создание новых категорий
   - Выбор иконки и цвета

6. **StatisticsScreen.js** (Статистика)
   - Графики расходов/доходов
   - Навигация по месяцам
   - Распределение по категориям

7. **SettingsScreen.js** (Настройки)
   - Выбор валюты
   - Выбор темы
   - Экспорт/сброс данных

### `src/components/` - UI компоненты

**11 переиспользуемых компонентов:**

```javascript
// Основные элементы
Card - информационные карточки
Button - кнопки (primary, secondary, outline, danger)
InputField - поле ввода текста
TransactionItem - элемент списка операции

// Селекторы
TypeSelector - выбор типа (доход/расход)
CategorySelector - сетка выбора категории
DatePicker - модальный выбор даты

// Специализированные
CategoryItem - элемент категории
SectionHeader - заголовок секции
EmptyState - пустое состояние списка
```

### `src/context/` - Управление состоянием

**AppContext.js** - центральное управление состоянием приложения

```javascript
// Состояние
transactions, categories, settings, isLoading

// Методы транзакций
addTransaction(), updateTransaction(), deleteTransaction()

// Методы категорий
addCategory(), updateCategory(), deleteCategory()

// Методы настроек
updateSettings(), setCurrency(), setTheme()

// Статистика
getBalance(), getMonthlyExpenses(), getMonthlyIncome()
getExpensesByCategory(), getIncomeByCategory()
```

### `src/services/` - Бизнес-логика

**StorageService.js** - работа с локальным хранилищем

```javascript
class StorageService {
  // Инициализация
  initialize()
  
  // Транзакции
  getTransactions()
  addTransaction()
  updateTransaction()
  deleteTransaction()
  getTransactionsByDateRange()
  
  // Категории
  getCategories()
  getCategoriesByType()
  addCategory()
  updateCategory()
  deleteCategory()
  
  // Настройки
  getSettings()
  updateSettings()
  getCurrency()
  setTheme()
  
  // Экспорт/Импорт
  exportData()
  importData()
  resetAllData()
}
```

### `src/utils/` - Утилиты

**dateUtils.js** - работа с датами
```javascript
formatDate() - преобразование даты в строку
formatDateTime() - дата и время
getMonthYear() - месяц и год
isToday() - проверка текущего дня
isThisMonth() - проверка текущего месяца
startOfMonth() - начало месяца
endOfMonth() - конец месяца
```

**moneyUtils.js** - финансовые операции
```javascript
formatCurrency() - форматирование суммы с символом валюты
calculateBalance() - расчет баланса
calculateCategoryTotal() - сумма по категории
calculateMonthlyExpenses() - расходы месяца
calculateMonthlyIncome() - доходы месяца
groupByCategory() - группировка по категориям
```

**validation.js** - валидация данных
```javascript
validateAmount() - проверка суммы
validateCategory() - проверка категории
validateComment() - проверка комментария
validateTransaction() - полная проверка операции
```

### `src/constants/` - Константы

**colors.js** - цветовые схемы
```javascript
lightTheme - светлая тема
darkTheme - темная тема
categoryColors - цвета для категорий
```

**categories.js** - категории
```javascript
DEFAULT_EXPENSE_CATEGORIES - стандартные расходы
DEFAULT_INCOME_CATEGORIES - стандартные доходы
```

**currencies.js** - валюты
```javascript
CURRENCIES - объект с информацией о валютах
DEFAULT_CURRENCY - валюта по умолчанию
```

### `src/navigation/` - Навигация

**Navigation.js** - настройка маршрутизации

```javascript
RootNavigator - корневой стек (Splash → MainStack)
MainStack - основная навигация (4 Tab Navigator вкладки)
DashboardStack - стек Dashboard экрана
TransactionsStack - стек операций
```

## 🔄 Поток данных (Data Flow)

### 1. Инициализация приложения

```
App.js
  └→ AppProvider (инициализация)
      └→ useEffect()
          └→ StorageService.initialize()
              └→ AsyncStorage.getItem()
              └→ Загрузка транзакций, категорий, настроек
              └→ Установка состояния в AppContext
```

### 2. Добавление операции

```
AddTransactionScreen
  └→ handleAddTransaction()
      └→ validateTransaction() ✓
          └→ useApp().addTransaction()
              └→ AppContext.addTransaction()
                  └→ StorageService.addTransaction()
                      └→ AsyncStorage.setItem()
                      └→ Возврат новой операции
                  └→ setTransactions([...old, new])
              └→ navigation.goBack()
```

### 3. Загрузка и отображение данных

```
DashboardScreen
  └→ useFocusEffect()
      └→ useApp() получает контекст
          ├→ getBalance() - расчет баланса
          ├→ getMonthlyExpenses() - расходы месяца
          ├→ getMonthlyIncome() - доходы месяца
          └→ getExpensesByCategory() - расходы по категориям
      └→ Отображение данных в UI
```

## 🔒 Управление состоянием

### Уровни состояния

```
┌─────────────────────────────────┐
│  App-level State (AppContext)   │
│  - transactions                 │
│  - categories                   │
│  - settings                     │
│  - isLoading                    │
└─────────────┬───────────────────┘
              │
        ┌─────▼──────┐
        │ useApp()   │
        └─────┬──────┘
              │
     ┌────────┴────────┐
     │                 │
┌────▼────┐    ┌──────▼────┐
│ Screens │    │ Components │
└─────────┘    └────────────┘

┌────────────────────────────┐
│ Local State (useState)     │
│ - form fields              │
│ - UI toggles              │
│ - local selections        │
└────────────────────────────┘
```

## 📊 Модель данных

### Транзакция

```javascript
{
  id: string,                    // Уникальный ID
  type: 'income' | 'expense',   // Тип операции
  amount: number,                // Сумма (0.01 - 999999.99)
  categoryId: string,            // ID категории
  date: ISO8601 string,         // Дата/время операции
  comment?: string,              // Опциональный комментарий (макс 200)
  createdAt: ISO8601 string,    // Дата создания
  updatedAt?: ISO8601 string    // Дата обновления
}
```

### Категория

```javascript
{
  id: string,                    // Уникальный ID
  name: string,                  // Название категории
  type: 'income' | 'expense',   // Тип операций
  icon: string (emoji),          // Иконка-эмодзи
  color: hex color,              // Цвет категории
  // Пользовательские категории имеют ID, начинающиеся с временной метки
  // Предустановленные начинаются с '1...' (расходы) или '10...' (доходы)
}
```

### Настройки

```javascript
{
  currency: string,              // Код валюты (USD, EUR, etc.)
  theme: 'light' | 'dark',      // Тема приложения
  notifications: boolean,        // Включены ли уведомления
}
```

## 🎯 Паттерны проектирования

### 1. Singleton Pattern (StorageService)

```javascript
// StorageService экспортируется как синглтон
export default new StorageService();

// Использование везде
import StorageService from '../services/StorageService';
StorageService.getTransactions();
```

### 2. Context API Pattern (AppContext)

```javascript
// Глобальное управление состоянием
const AppContext = createContext();
const AppProvider = ({ children }) => {
  // Логика управления состоянием
  return <AppContext.Provider value={value}>{children}</AppContext.Provider>;
};

// Использование в компонентах
const { transactions, addTransaction } = useApp();
```

### 3. Composition Pattern (Components)

```javascript
// Простые, переиспользуемые компоненты
<View>
  <Card title="Balance" value="$1000" />
  <Button title="Add" onPress={handleAdd} />
  <TransactionItem transaction={t} />
</View>
```

### 4. Validation Pattern (Utils)

```javascript
// Отдельный слой валидации
const { isValid, errors } = validateTransaction(transaction);
if (!isValid) {
  setErrors(errors);
  return;
}
```

## 🔧 Расширение функциональности

### Добавление новой категории операций

1. Добавить в `constants/categories.js`:
```javascript
export const NEW_CATEGORIES = [
  { id: 'new_1', name: 'Категория', color: '#000000', icon: '🎯', type: 'expense' }
];
```

2. Использовать в компоненте:
```javascript
<CategorySelector categories={[...DEFAULT_CATEGORIES, ...NEW_CATEGORIES]} />
```

### Добавление новой валюты

1. Добавить в `constants/currencies.js`:
```javascript
export const CURRENCIES = {
  // ... существующие
  NEW_CODE: { code: 'NEW', symbol: '💱', name: 'Новая валюта' }
};
```

2. Использовать автоматически в SettingsScreen:
```javascript
{Object.entries(CURRENCIES).map(([code, currency]) => (
  <CurrencyOption key={code} currency={currency} />
))}
```

### Добавление новой утилиты

1. Создать в `src/utils/`:
```javascript
// newUtils.js
export const myFunction = (data) => {
  // логика
};
```

2. Использовать в компонентах:
```javascript
import { myFunction } from '../utils/newUtils';
```

## ⚡ Оптимизация производительности

### 1. useFocusEffect для обновления экрана

```javascript
useFocusEffect(
  useCallback(() => {
    // Обновляется при каждом фокусе на экран
    const data = loadData();
    setData(data);
  }, [])
);
```

### 2. Мемоизация в AppContext

```javascript
const getBalance = useCallback(() => {
  return calculateBalance(transactions);
}, [transactions]); // Пересчитывается только при изменении
```

### 3. Группировка операций по датам

```javascript
// Группируем данные перед отображением
const grouped = groupByDate(transactions);
// Отображаем по одной дате за раз
```

## 📚 Дополнительные ресурсы

### React Native документация
- https://reactnative.dev/docs/getting-started
- https://reactnative.dev/docs/asyncstorage

### React Navigation
- https://reactnavigation.org/docs/getting-started
- https://reactnavigation.org/docs/bottom-tab-navigator

### Expo
- https://docs.expo.dev/
- https://docs.expo.dev/get-started/create-a-new-app/

---

**Версия документации:** 1.0  
**Дата:** 2024-2025  
**Уровень сложности:** Средний
